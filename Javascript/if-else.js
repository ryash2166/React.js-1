// let num = peompt("olease enter Number");

// if (num % 2 == 0) {
//  document.hetElementByid("demo").innerHTML =
//   "Your Number is " + num" + even.";
// } 
// else{
// documnet.gteElementById("demo").innerHTML=
// "your Number is " + num + " add."";  
// }

     // Q-2

// let num1 = prompt(), num2 = prompt(), largest;

// if (num1 >= num2) {
//   largest = num1;
// } else {
//   largest = num2;
// } 

// console.log("The largest number is: " + largest);

      // Q-3

      // {
      //   let x = prompt("Enter Number 1:");
      //   let y = prompt("Enter Number 2:");
      //   let ch = parseInt(prompt("Enter your choice:"));
        
      //   switch (ch) {
      //     case 1:
      //     res = x + y;
      //     console.log("\nResult is :" + res)
      //     break
    
      //     case 2:
      //     res = x - y
      //     console.log("\nResult is :" + res)
      //     break
    
      //     case 3:
      //     res = x * y
      //     console.log("\nResult is :" + res)
      //     break
    
      //     case 4:
      //     res = x / y
      //     console.log("\nResult is :" + res)
      //     break
    
      //     case 5:
      //     res = x % y;
      //     console.log("\nResult is :" + res)
      //     break
    
      //     default:
      //     console.log("Invalid Choice:" + ch)
      //   }
      // }
    

      // Q-4

  // let m = prompt();

  // if (m <=35){
  //   console.log("You Are Failed.");
  // }
  // else if (m >= 36 && m <= 55){
  //   console.log("D Grade.");
  // }
  // else if (m >= 55 && m <= 70){
  //   console.log("C Grade.");
  // }
  // else if (m >= 71 && m <= 80){
  //   console.log("B Grade.");
  // }
  // else{
  //   console.log("A Grade.");
  // }
  
      //Q-5

      // Assigning values to variables
      var y = 3;
      var x = 1;
      var z = 2;

// Checking the conditions to determine the order of variables
if (x > y && x > z) {
    if (y > z) {
        console.log(x + ", " + y + ", " + z);
    } else {
        console.log(x + ", " + z + ", " + y);
    }
} else if (y > x && y > z) {
    if (x > z) {
        console.log(y + ", " + x + ", " + z);
    } else {
        console.log(y + ", " + z + ", " + x);
    }
} else if (z > x && z > y) {
    if (x > y) {
        console.log(z + ", " + x + ", " + y);
    } else {
        console.log(z + ", " + y + ", " + x);
    }
} 

      Q-6
    {
      let a = -9;
      let b = 3;
      console.log(a*b);
    }

       {
          let a ="Value is"
          let b = 50

          document.write(`"${a}  ${b}"`)
       }
    {
      let a = 17;
      let b = 5;
      console.log(a%b);
    }

    {
      let a = 5;
      let b = 17;
      console.log(a%b);
    }

    {
      let a = 5;
      let b = 10;
      console.log(a/b);
    }

    {
      let a = 4;
      let b = 4;
      console.log(a==b);
    }

    {
      let a = 4;
      let b = 5;
      console.log(a!=b);
    }

    {
      let a = 7;
      let b = 8;
      console.log(a<=b);
    }